# 敏感度重測與升溫模式案例研究

紀錄日期：本次對話當下（`melter2` 專案 `#1/#2/#3` 三項 bug 修復之後）。
執行方式：`python -m src.sensitivity_analysis`（產生 `SENSITIVITY_REPORT.md`）+ 手動案例比較腳本。

## 1. 敏感度重測結果

再次執行完整的敏感度分析（21 組參數掃描 + `pytest tests/` 共 24 個測試）驗證：

- 全部 21 項掃描的「方向」判定跟前次一致；`#1`（`HEARTH_AREA_M2`）、`#2`
  （`STOICH_AIR_GAS_RATIO` 死 import）、`#3`（`theoretical_gas_nm3` 讀錯 `GAS_LHV` 來源）
  三個 bug 修復後的項目全部維持 ✅ matches，沒有出現新的異常。
- 24 個 pytest 測試（`tests/test_optimizer.py` + `tests/test_sensitivity.py`）全數通過。
- 仍保留的 2 個「非 bug、但值得注意」的模型行為特性，跟上次相同（因為沒有改動相關邏輯，
  本來就預期不變）：
  1. `burnoff_ea`（Arrhenius 活化能）過低（<30,000 J/mol）時，燒損模型會算出物理上不合理的
     數值（超過投料量 40%），目前沒有防呆機制，但校正值（45,000）本身沒問題。
  2. `target_bath_temp_c` 在**固定式排程**下，若排程本身已經燒過頭撞到 800°C 硬上限，目標溫度
     會失去作用（因為 Melt 階段不論湯溫是否達標都會持續加熱）。已確認這**不影響**實際部署的
     `optimize_heating_curve()` 最佳化搜尋（它會直接剔除超標/未達標的候選排程），只影響單獨
     呼叫 `simulate_trajectory()` 且排程本身設計不良的情況。

詳細的逐參數方向判定、預期值來源（手冊條文/真實產量數據）與數值，見同目錄下的
`SENSITIVITY_REPORT.md`（每次執行 `python -m src.sensitivity_analysis` 會自動重新產生）。

## 2. 案例研究：現有升溫模式 vs. 建議優化升溫模式

### 情境設定

具代表性的真實情境：**65 噸 5083 合金、6 小時出湯時限、7 噸前爐殘湯、目標湯溫 720°C**
（5083 是 MFA 產線實際產量最多的合金家族，約佔全部爐次 31%；7 噸殘湯落在現場經驗回報的
5～10 噸範圍中間值）。

- 現有模式：`run_baseline_scenario()`，固定頂溫設點 1100°C、25% 過剩空氣（對應手冊所述
  「頂溫常設為一高固定值、之後不再調整」的常見實務做法）。
- 建議優化模式：`optimize_heating_curve()` 在滿足「必須於 6 小時內達到 720°C ± 5°C」這個
  硬性限制的前提下，搜尋成本最低（燃氣+氧化燒損）的頂溫/切換點/保溫設點/過剩空氣組合。

### 比較結果

| 項目 | 現有模式（傳統固定 1100°C） | 建議優化模式 | 差異 |
|---|---|---|---|
| 頂溫設點（Melt 階段） | 1100°C，維持 5.4 小時（90%工時） | 1120°C，僅維持 2.7 小時（45%工時） | 優化模式提早切換到保溫階段 |
| 保溫階段設點 | 依湯溫誤差降到 700°C（因已過熱，誤差判定為 0） | 依湯溫誤差先衝到 880°C 再降到 700°C | 優化模式的保溫段真正發揮「追蹤目標」功能 |
| 過剩空氣率 | 25%（超出手冊 15% 高燃設計上限） | 10%（低於上限） | 優化模式更接近手冊建議值 |
| 煙道殘氧 O₂ | 3.47%（超出手冊 AT104 實務目標 1-3%） | 1.9%（落在手冊目標區間內） | 現有模式供氣過量 |
| **最終湯溫** | **800.0°C**（撞到模型安全上限，超出手冊 690-750°C 服務溫度上限 50°C） | **720.2°C**（幾乎精準命中目標） | 現有模式明顯「燒過頭」 |
| 燃氣用量 | 3,886.6 Nm³ | 2,436.9 Nm³ | 省 1,449.6 Nm³（-37%） |
| 氧化燒損 | 1,873 kg（約佔投料 2.9%） | 723 kg（約佔投料 1.1%） | 省 1,150 kg（-61%） |
| 燃氣成本 | 58,299 元 | 36,554 元 | — |
| 燒損成本 | 140,472 元 | 54,195 元 | — |
| **總成本** | **198,771 元** | **90,749 元** | **省 108,022 元（-54.3%）** |

### 關鍵發現（不只是省瓦斯）

把逐時刻的爐溫軌跡攤開來看：

- **現有模式**：t=4 小時湯溫就已到達模型的 800°C 硬上限，但頂溫設點仍維持在 1100°C 又燒了
  1.4 小時才切到保溫——這段時間燃氣其實是在對一鍋已經過熱的鋁湯「空燒」，沒有任何實質效益，
  只有持續氧化燒損。
- **優化模式**：t=3 小時湯溫剛好碰到 720°C 附近，保溫階段的誤差回饋機制立刻把頂溫設點壓回
  700°C，之後只需要極低瞬時流量（約 22 Nm³/h）維持溫度，符合手冊「Idle 模式」的設計精神。

### 合理性檢查

優化結果的殘氧 1.9%、過剩空氣 10%、最終湯溫 720.2°C 都精準落在 Mechatherm 手冊記載的實務
數值範圍內，不是憑空跑出來的極端值：
- 過剩空氣 10% < 手冊 15% 高燃設計上限
- 殘氧 1.9% 落在手冊 AT104 實務目標 1-3% 區間內
- 最終湯溫 720.2°C 落在手冊 690-750°C 服務溫度範圍內

### 誠實的限制說明

這裡的「現有模式」是程式內建的代表性基準情境（固定 1100°C／25% 過剩空氣），並非逐爐重播的
真實歷史操作紀錄（因為現場沒有留存每爐真實頂溫設點的歷史資料）。不過支撐這個比較的物理常數
（`efficiency_scale`、`burnoff_k0`）已經用 123 筆真實產量資料校正過（燃耗 MAPE 17.8%），所以
數量級是有真實資料支撐的，只是不是逐爐一對一重播。詳見 `PLAN.md` 與 `MELTER_KNOWLEDGE.md`。

## 重現方式

```bash
# 重新執行敏感度分析並更新 SENSITIVITY_REPORT.md
python -m src.sensitivity_analysis

# 重新執行單元測試
pytest tests/ -v

# 重現本案例研究的數字
python -c "
from src.optimizer import HeatingCurveOptimizer
opt = HeatingCurveOptimizer()
res = opt.optimize_heating_curve(65000.0, 6.0, alloy_name='5083', target_bath_temp_c=720.0, residual_weight_kg=7000.0)
print(res['baseline_summary'])
print(res['optimal_summary'])
print(res['optimal_params'])
print(res['savings'])
"
```
