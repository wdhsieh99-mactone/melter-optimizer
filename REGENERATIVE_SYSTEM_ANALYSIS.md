# 蓄熱式燃燒系統：因子影響排名、燒嘴同時開火釐清、蓄熱介質子模型可行性評估

## 1. 固定出料量／出料時間下，各因子影響程度排名

情境：65 噸、5052、6 小時出湯時限、720°C 目標湯溫，操作點取最佳化搜尋結果
（頂溫 1120°C、切換點 2.7h、保溫 880°C、過剩空氣 10%）。對每個因子做 ±20% 擾動
（決策變數以偏離最佳值衡量），觀察總成本（燃氣+氧化燒損）變化幅度：

| 排名 | 因子 | -20% → 成本變化 | +20% → 成本變化 | 平均影響幅度 |
|---|---|---|---|---|
| 1 | 頂溫設點 `sp_roof_melt` | -38.6% | +59.0% | **48.8%** |
| 2 | 爐床輻射面積 `HEARTH_AREA_M2` | -22.2% | +28.8% | **25.5%** |
| 3 | 切換保溫時機 `t_switch_hrs` | -12.2% | +15.1% | 13.6% |
| 4 | 輻射係數 `emissivity_eff` | -11.4% | +13.9% | 12.6% |
| 5 | 氧化燒損係數 `burnoff_k0` | -10.9% | +10.9% | 10.9% |
| 6 | 燃料熱值 `GAS_LHV` | +10.4% | -7.4% | 8.9% |
| 7 | 空燃比 `excess_air_pct` | -5.3% | +4.7% | 5.0% |
| 8 | 燃燒效率校正 `efficiency_scale` | +6.6% | 0.0%＊ | 3.3% |
| 9 | 保溫設點 `sp_roof_hold` | 0.0% | +6.5% | 3.3% |
| 10 | 爐體固定散熱 `wall_loss_kw` | -0.4% | +0.4% | 0.4% |

＊`efficiency_scale` +20% 顯示 0% 效應，是因為目前校正值（1.248）已讓 `combustion_efficiency()`
頂到模型設定的效率上限 0.68，超過上限後再提高係數不會有進一步效果——這是校正值「用滿」模型
允許空間的訊號，值得留意但不影響方向性正確。

**結論**：使用者點名的「空燃比」與「燃料熱值」實際排名第 6、7，真正主導成本的是**頂溫設點**
與**爐床輻射面積**——這兩者都直接放大傳熱速率，而目前 Melt 階段不論湯溫是否達標都持續加熱
的設計（見 `CASE_STUDY.md`），會把任何放大傳熱速率的因子的影響進一步放大。實務意涵：精準
控制頂溫排程與切換時機，比精準校準空燃比更能左右成本。

## 2. 蓄熱式燃燒系統在模型內的現況

- **配對切換（2 對 4 燒嘴）**：`optimizer.py` 的 `burner_mode` 欄位區分「Dual Pair」
  （Melt 階段，`MAX_GAS_FLOW_DUAL_PAIR=1300 Nm³/h`）與「Single Pair Duty Toggle」
  （Hold 階段，`MAX_GAS_FLOW_SINGLE_PAIR=650 Nm³/h`）——**有**建模。
- **蓄熱回收效益**：`combustion_efficiency()` 的基準值 0.65（註解：「70% regenerative
  waste heat recovery」）是蓄熱回收效益的唯一體現，被壓縮成一個固定係數，再乘上校正因子
  `efficiency_scale`。
- **沒有建模的部分**：60-120 秒的實際交替換向週期、蓄熱球本身的蓄熱/放熱動態、兩對燒嘴各自
  獨立的溫度或流量、換向瞬間的壓力擾動——見第 4 節，這部分其實**有真實感測器資料可用**，
  只是目前完全沒被使用。

## 3. 手冊釐清：四隻燃燒器可以同時開嗎？

**不行。手冊明確排除這個可能性。** 原文（`1.4.56 C 151, 152 Regenerative Cycle Loop
Functions`）：

> "Only one burner will fire at any one time, its twin will be in exhaust mode drawing
> the furnace exhaust products through its regenerative bed recharging the temperature
> of bed media."

這句話是**針對每一對**燒嘴說的——同一對裡永遠只有一支在燒，另一支在排氣蓄熱。所以：

- **穩態下最多同時 2 支燒嘴在燒**（兩對各出一支），不是 4 支。
- 唯一的例外是「Low Fire Change Over」換向瞬間，同一對內會有 1-2 秒的「both burners are
  on」重疊（為了換向時火焰不斷），但這是**同一對內部**的短暫重疊，不代表跨兩對同時 4 支點火，
  且僅維持約 1-2 秒（見手冊 cycle lead/lag delay 參數，預設各 0.5 秒）。
- 手冊 `1.4.22`：「the duty burner pair will remain on, this duty burner is automatically
  toggled between the two burners」——低燃/保溫時只留一對值班，該對內仍持續交替點火，另一對
  完全關閉。

**已修正**：原本程式碼裡的標籤「Dual Pair (4 Burners)」用詞不精確——`MAX_GAS_FLOW_DUAL_PAIR
=1300 Nm³/h` 這個**總燃氣輸入能力數字**作為「兩對同時服役」的聚合代表值，方向上沒問題（因為
即使物理上只有 2 支同時點火，整個燃燒週期內兩對合計的平均熱輸入能力仍大致對應這個量級），
但標註成「4 Burners」容易讓人誤以為 4 支燃燒器同時點火，跟手冊描述的蓄熱原理矛盾。

已修正的位置：
- `src/optimizer.py`：`burner_mode` 改為「Dual Pair (up to 2 Flames Alternating)」/
  「Single Pair (1 Flame Alternating)」
- `src/physics_model.py`：`MAX_GAS_FLOW_DUAL_PAIR`/`MAX_GAS_FLOW_SINGLE_PAIR` 註解同步修正
- `app.py`：圖表標題、分頁文字，以及手冊分頁新增手冊原文引用（1.4.56："Only one burner will
  fire at any one time"）與實測 240 秒週期數字（取代原本用猜測填的 60-90 秒）
- `MELTER_KNOWLEDGE.md`：對應段落同步更新

程式碼行為（`simulate_trajectory` 的耗氣量/成本計算）**完全沒有改變**，純粹是文字/標籤修正。

## 4. 蓄熱介質儲熱狀態子模型：可行性評估

### 4.1 好消息：感測器 CSV 裡已經有對應的真實資料，而且完全沒被用過

`mfa_20260707-0714_wide.csv` 裡的 `mfa_tt111_pv` ~ `mfa_tt114_pv` 四個欄位（`MELTER_KNOWLEDGE.md`
原本標註為「未確認，推測與燒嘴/蓄熱相關」），這次實際檢驗發現：

- `tt111` 與 `tt112` 相關係數 **-0.96**（幾乎完美反向），`tt113` 與 `tt114` 相關係數 **-0.927**
  ——正是「同一對裡一支升溫（蓄熱/排氣中）、另一支降溫（放熱/點火中）」的訊號特徵，跟手冊描述
  的交替蓄熱機制完全吻合。編號也恰好對應手冊的 `BC 111,112,113,114` 燒嘴控制器迴圈編號，
  與 `app.py` 手冊分頁裡原本就寫的「Pair 1: 111/112, Pair 2: 113/114」猜測一致。
- `tt111` 與 `tt113` 相關係數 **+0.96**（強烈同向）——顯示兩對燒嘴的換向動作是**同步**的，
  不是各自獨立亂序切換。
- 用峰值偵測量出完整週期約 **215 秒**（16 個週期間隔幾乎全部落在 200-215 秒，非常穩定）。
  這跟手冊 `1.4.56` 描述的「最大燃燒週期 HMI 可調 80-120 秒（預設 60 秒）」對得起來——若本廠
  實際設定值接近上限（約 107 秒/半週期），一次完整來回（燒嘴A燒完換B、B燒完再換回A）恰好
  ≈ 2×107 ≈ 215 秒，跟量到的數字幾乎完全吻合。
- 數值範圍約 80-190°C，振幅約 60-110°C，遠低於煙道/頂溫，但明顯高於室溫，跟「預熱後的燃燒
  空氣溫度」或「蓄熱介質局部溫度」的量級相符（尚待跟現場工程確認精確物理意義，但訊號行為
  極具說服力）。

### 4.2 子模型設計方向（若要往下做）

有了上面的實測訊號，建立一個「蓄熱介質儲熱狀態」子模型是可行的，大致架構：

1. **狀態變數**：4 個燒嘴各自的蓄熱介質集中溫度 `T_bed_111...114`（一階集總熱容模型）。
2. **充熱（排氣/蓄熱中）**：`T_bed` 以一階遲滯趨近當下爐內排氣溫度，時間常數由蓄熱介質質量
   （手冊已有數字：每組蓄熱床約 3,680kg，介質約 1,600kg）與排氣流量/熱傳係數決定——熱傳係數
   可用剛才量到的振幅（~60-110°C）與週期（~215秒）反推校正，不用憑空假設。
3. **放熱（點火/預熱空氣中）**：`T_bed` 下降，釋出的熱量預熱進入該燒嘴的冷空氣，預熱後空氣
   溫度即可取代目前 `combustion_efficiency()` 裡那個固定的「0.65 基準值」，變成真正由蓄熱
   狀態動態決定的燃燒效率，而不是一個時間不變的經驗係數。
4. **異常注入點**：把特定燒嘴的蓄熱介質「有效熱傳係數」或「有效熱容」調低（模擬阻塞/破損/
   偏流），模型就會自然重現：該燒嘴振幅萎縮或平均溫度偏移、兩對之間出現不對稱、整體
   `combustion_efficiency` 下降、同樣加熱需求下耗氣量上升——剛好對應上次討論的異常特徵，
   而且現在是可以真正**量化模擬**，不只是文字描述。

### 4.3 誠實的範圍評估

這不是像 `#1/#2/#3` 那樣的小修復，是一個新的中等規模子模型，需要：

1. **確認訊號物理意義**：目前的判讀（預熱空氣溫度或蓄熱介質溫度）是根據行為模式強力推論，
   建議如果有機會應向現場工程確認 `tt111-114` 的儀表位號定義。
2. **建立並校正一階熱容模型**：用上面量到的振幅/週期/相位關係去反推熱傳係數，這部分方法上
   跟 `calibration.py` 對整爐能量平衡的做法類似，但校正目標不同（這次是校正蓄熱床本身）。
3. **決定是否要接回 `combustion_efficiency()`**：把現在的固定經驗公式換成由蓄熱狀態動態
   決定，是一個會影響既有校正結果的實質物理模型變更，需要重新驗證，跟先前 `STOICH_AIR_GAS_RATIO`
   討論時的謹慎態度一樣。
4. **設計「健康指標」輸出**：例如即時比較 `tt111-114` 的振幅/週期/對稱性跟校正過的健康基準，
   偏離超過門檻就示警——這才是真正對應「蓄熱箱異常」偵測需求的產出。

**建議**：先做第 1-2 步（訊號解讀確認＋建立/校正熱容子模型，這步驟本身就能拿真實資料驗證，
風險低、產出可獨立使用），再視結果決定要不要進到第 3 步（接回主要成本模型）。第 4 步（健康
指標）可以完全獨立於第 3 步先做，且更貼近手冊本身「趨勢監控抓異常」的實務邏輯。

## 5. 第 1-2 步實作結果（`src/regenerator_model.py`）

### 5.1 週期重新驗證：215 秒的估計要修正為 240 秒

原本用單一時窗估計的 ~215 秒只是雜訊；把整週資料切成 132 個 1 小時視窗逐一量測後：
- **119 個視窗（90%）的週期緊密集中在 240.0 秒**（平均 240.7s，標準差僅 3.9s）
- 其餘 13 個視窗全部對應**低燃/接近停爐**的時段（平均燃氣流量遠低於正常值）——此時蓄熱循環
  訊號本來就不規則，量到的「週期」是雜訊，不是真實蓄熱循環，已在模型中以
  `MIN_ACTIVE_GAS_FLOW_NM3H` 門檻排除
- 240 秒 = 2×120 秒，恰好對到手冊 `1.4.56` 最大燃燒週期 HMI 可調範圍（80-120 秒/半週期）的
  **上限**——研判本廠是把此參數設定在可調範圍頂端，且在正常燃燒時是固定週期運轉，而非手冊
  描述的「達到蓄熱排氣溫度設定值就提前換向」的溫度自適應模式（120 個乾淨視窗內週期與燃氣
  流量相關係數僅 -0.246，太弱，不支持溫度自適應假說）

### 5.2 一階熱容模型與擬合品質

模型採用週期性繼電熱容曲線：燃燒中的燒嘴溫度以時間常數 `tau_discharge` 指數趨近低溫端，
排氣蓄熱中的燒嘴以 `tau_charge` 趨近高溫端，每半週期反轉一次。**擬合視窗長度是關鍵**：
- 用 2 小時長視窗直接擬合，R² 只有 0.38-0.43（爐溫/頂溫在 2 小時內會漂移，不符合「穩態週期」假設）
- 縮短到 20 分鐘（~5 個週期）視窗後，R² 提升到 0.78-0.98

實作過程中還修正了兩個真正的擬合缺陷（不是資料問題，是我自己寫的擬合邏輯的 bug）：
1. **T_high/T_low 標籤翻轉**：`curve_fit` 沒有強制「高溫端>低溫端」，導致不同視窗的擬合結果
   偶爾整組被「對調」（同一條曲線、不同標籤），把不同正負號的振幅混在一起算統計會嚴重高估
   變異數。修正：擬合後若偵測到標籤顛倒，整組（T_high/T_low、tau_discharge/tau_charge、
   相位）一起對調，數學上驗證過 R² 完全不變。
2. **單次擬合陷入局部最優**：即使用零雜訊的合成訊號測試，單次 `curve_fit` 也只能拿到
   R²=0.984、`tau_discharge` 誤差達 32%（真值 50 秒，擬得 66 秒）——說明 (T_high, T_low, tau,
   相位) 在單一 120 秒半週期內互相高度相關，容易卡在錯誤但看起來也不差的解。修正：改成
   多起點擬合（2 種相位起點 × 3 種 tau 起點，共 6 組），取 R² 最高者。修正後同一組零雜訊測試
   資料可以精確擬合回真值（R²=1.0000），單次擬合仍維持在 <0.2 秒內完成。

### 5.3 校正後的健康基準（全週 312 個有效視窗，1,248 次燒嘴擬合，853 次 R²≥0.7）

| 燒嘴 | 振幅 (°C) | tau_discharge (s) | tau_charge (s) | 樣本數 |
|---|---|---|---|---|
| tt111（第1對-A） | 93.9 ± 9.1 | 52.9 ± 5.7 | 53.9 ± 5.4 | 222 |
| tt112（第1對-B） | 68.1 ± 8.4 | 51.9 ± 5.1 | 54.0 ± 4.7 | 218 |
| tt113（第2對-A） | 76.6 ± 10.3 | 51.5 ± 5.1 | 53.6 ± 5.1 | 209 |
| tt114（第2對-B） | 63.7 ± 9.3 | 52.8 ± 4.0 | 52.6 ± 4.1 | 204 |

**修正過去的一個假設**：原本認為「健康的一對燒嘴應該振幅/tau 幾乎對稱」，但全週資料顯示
tt111 的振幅（93.9°C）持續且穩定地比雙胞胎 tt112（68.1°C）大了約 26°C，兩對燒嘴皆然——
這比對稱性假設本身的標準差大了 2-3 倍，代表這是**真實存在、穩定的先天不對稱**（可能是管路
長度、燒嘴方向或感測器安裝位置造成），不是異常。反而是四支燒嘴的 `tau_discharge`/`tau_charge`
高度一致（52-54 秒，變異係數僅約 10%），符合「同一款蓄熱床設計、應有相近熱時間常數」的物理
預期，這點更適合作為異常比對基準。程式碼裡 `pair_symmetry_error()` 的說明文字已同步修正，
標註為「輔助診斷、不能當作絕對門檻」；真正的異常判斷改用 `regenerator_health_index()`——
每支燒嘴各自對照自己的歷史基準算 z-score，而不是要求兩支燒嘴互相對稱。

### 5.4 健康指標驗證

用合成的「劣化」訊號（把某支燒嘴的振幅硬壓到只剩健康值的 25%，模擬蓄熱介質阻塞/偏流導致
熱交換能力大幅下降）餵給 `regenerator_health_index()`，成功正確標記為 `ANOMALY`
（z-score 遠超過門檻），而同一支燒嘴的正常資料維持 `OK`。測試見
`tests/test_regenerator_model.py::test_health_index_flags_synthetic_degraded_bed`。

### 5.5 產出檔案

- `src/regenerator_model.py` — 完整子模型：擬合、多起點優化、週期偵測、全週基準校正、
  健康指標比對、基準值存讀（`src/regenerator_baseline.json`）
- `tests/test_regenerator_model.py` — 7 個測試，涵蓋合成訊號精確還原、真實資料迴歸防護
  （反相關係數、週期）、健康指標異常偵測驗證
- 執行 `python -m src.regenerator_model` 會重新掃描整週資料、重新校正基準值並存檔
  （約需 2 分鐘，多起點擬合的代價換來乾淨許多的結果）

### 5.6 尚未做的部分（第 3-4 步，仍待決定範圍）

- **尚未**把這個子模型接回 `combustion_efficiency()`——目前仍是獨立、旁路的診斷工具，不影響
  `src/optimizer.py`/`src/calibration.py` 既有的校正結果。要接回去是一次會影響既有計算結果
  的實質物理模型變更，維持先前討論 `STOICH_AIR_GAS_RATIO` 時的謹慎態度，需要另外評估。
- **尚未**跟現場工程確認 `tt111-114` 的儀表位號定義——目前的物理意義判讀（預熱空氣或蓄熱
  介質溫度）是根據訊號行為（反相關、週期、振幅量級）強力推論所得，行為證據充分，但沒有
  儀表清單佐證。
- 健康指標目前只能用歷史資料手動觸發（呼叫 `regenerator_health_index()`），沒有接上即時資料
  流或 `app.py` 介面顯示——如果要在操作員儀表板上看到即時「蓄熱箱健康燈號」，還需要額外的
  介接工作。
