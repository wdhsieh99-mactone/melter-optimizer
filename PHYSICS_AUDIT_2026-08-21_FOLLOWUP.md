# 80T 鋁熔解爐模型 — 物理錯誤鑑別與修訂方案（第二輪）

**審查基準**：worktree `physics-review` @ `798587a`（父 commit `612af99` = v2.3.1）
**日期**：2026-08-21
**定位**：`PHYSICS_AUDIT_2026-08-20.md` 的後續複驗 + 新增問題 + 可執行修訂方案
**狀態**：診斷完成，尚未動工

---

## Context

`melter2` 是一套 Streamlit app（桌面版 `app.py` + 手機版 `app_mobile.py`），用熱力學模型模擬 80T 靜置式鋁熔解爐的三段升溫曲線，並宣稱能相對「傳統做法」省下 40%+ 的瓦斯與燒損成本。**使用者裁決：本工具定位為現場操作指導工具**（不是純文件佐證），因此排序準則是「錯的建議會不會害到現場」，其次才是準度。

repo 內已有 `PHYSICS_AUDIT_2026-08-20.md`（釘選在同一 commit）指認了 A + B1–B9 共 10 項物理問題。**本輪複驗確認：其中 9 項在 HEAD 上完全未修，1 項（B8）僅部分處理。**

本文件不重複論證那 10 項，而是：

1. 用**實跑數字**把「未修」量化到可裁決的程度（不是「有疑慮」，是「差 33%」「高 515°C」「差 8.94%」）；
2. 補上既有 audit **沒抓到**的 6 項（含 2 項校正迴路結構缺陷、3 項手機版獨有問題）；
3. 提出**依決策影響排序、有執行順序相依、有驗收條件**的修訂方案。

---

## 結論先行

| # | 陳述 | 證據 |
|---|---|---|
| 1 | **出貨的 app 跑的物理常數，跟校正時用的不是同一組。** ε 0.85 vs 0.45（輻射差 1.89×）、LHV 37256 vs 40585、燒嘴上限 880 vs 1300，而 `efficiency_scale=1.2353` 是在後者擬合的 → **app 實質上未校正** | `app.py:348,355,378,385` vs `calibration.py:77,106` |
| 2 | **模型的「傳統做法」基準本身就比真實爐子省 23%。** app 實跑 baseline 53.7 Nm³/t、最佳解 46.4 Nm³/t；實測中位 **70.0 Nm³/t**（n=318）。基準是虛構的，其上的節省幅度無意義 | 實跑；`PHYSICS_AUDIT:106` |
| 3 | **氧化放熱變成爐子的主要熱源。** 一爐 39.9 GJ = **燃料熱的 37.6%**，隱含 ~1,285 kg 鋁被完全氧化；而實測 `yield_loss_pct` 中位僅 **1.12%**（25% 分位精確 0，n=280），且其中相當部分是浮渣夾帶的金屬而非氧化物。後果：終點湯溫 **1295°C**（手冊服役上限 750°C），112/391 時步湯溫高於爐頂溫（最大高 515°C） | 實跑 `simulate_trajectory` |
| 4 | **手機版把模擬湯溫標成 `TT200`。** `app_mobile.py:643` 圖例寫「現行鋁湯溫度 (TT200)」——把模擬曲線掛上真實熱電偶位號呈現給操作員，而那條曲線終點在 1295°C | `app_mobile.py:642-643` |
| 5 | **手機版與桌面版用不同的天然氣熱值。** `app_mobile.py:232` 傳 `gas_lhv_kj_nm3=`，但建構子的參數名是 `gas_lhv=`（`physics_model.py:112`），被 `**kwargs` 靜默吞掉 → 手機版跑 **40585**、桌面版跑 **37256**，**同一爐差 8.94%** | 實跑驗證 |
| 6 | **「最佳解」6 個參數有 5 個落在搜尋網格的邊界（全部是下界）。** 目標函數對每個決策變數都單調，沒有內部最適解 → 建議配方是網格畫在哪裡的產物 | 實跑 `optimize_heating_curve` |
| 7 | **建議的空燃比在現場實際操作點之外（安全項）。** 整個網格 `[10,12.5,15]%` 過剩空氣 → flue O₂ 1.90/2.32/2.72%，**全部低於廠內 O₂ trim 預設 3%**；實測操作點在 EA 49–57%。模型無 CO/未燃/火焰穩定懲罰項 | 實跑 + `MELTER_KNOWLEDGE.md:95-101` |
| 8 | **能量守恆測試是恆等式，永遠不會失敗。** `residual_error_gj` 由代數推導恆為 0；`test_phase1_sankey_strict_energy_conservation` 三條斷言全是套套邏輯 | `physics_model.py:317-334`；`tests/test_optimizer.py:186-200` |
| 9 | **overhead 被計了兩次。** 校正把「不含 overhead 的模擬耗氣」對到「實測總耗氣」，app 再把 overhead 加上去 | `calibration.py:90,99` vs `optimizer.py:488,496` |

**一句話**：目前的數字不是「不夠準」，是**每一層都有可證偽的算術或結構錯誤**。作為現場工具，它給出的空燃比建議與湯溫曲線都不能照做。

---

## 第一部分：問題鑑別

### 分層原則

| 層 | 定義 | 能否只靠算術/實跑證偽 |
|---|---|---|
| **L0 硬錯誤** | 單位、化學計量、常數不一致、重複計算 | ✅ 可，無需引文獻 |
| **L1 結構限制** | 模型架構決定了它不可能對 | ✅ 可實跑展示 |
| **L2 未驗證主張** | 缺資料或缺實驗設計 | ❌ 需新增驗證 |

---

### L0 硬錯誤（可用算術獨立確認）

| # | 問題 | 位置 | 證據 | 既有 audit |
|---|---|---|---|---|
| **L0-1** | **執行期常數 ≠ 校正期常數**。calibration 建模時不傳 `emissivity_eff`/`gas_lhv`/流量上限，吃 code default（0.45 / 40585 / 1300）；app 從 config 傳入（0.85 / 37256 / 880）並再度強制覆寫屬性。三項全不同 | `app.py:348,355,373,378,385,392`；`calibration.py:77,106-111` | `radiant_heat_flux_kw(1180,700)`：ε=0.45→6012 kW；ε=0.85→**11357 kW**。唯一活著的校正通道只剩 `efficiency_scale`（config 無此鍵） | B3（僅列 ε/LHV，**漏了流量上限**） |
| **L0-1b** | **`app_mobile.py` 有第三條獨立建構路徑**，且 `gas_lhv_kj_nm3=` 是**錯的參數名**（建構子接受 `gas_lhv=`），被 `physics_model.py:114` 的 `**kwargs` 靜默吞掉 | `app_mobile.py:224-243`；`physics_model.py:112,114` | 實跑：`MelterPhysicsModel(gas_lhv_kj_nm3=37256)` → `GAS_LHV = 40585`；`MelterPhysicsModel(gas_lhv=37256)` → `37256`。**手機版與桌面版對同一爐算出的燃料能量差 8.94%**。`**kwargs` 讓所有拼錯的參數名都不會報錯 | ❌ **新增** |
| **L0-1c** | **手機版把模擬結果標上真實位號**。`app_mobile.py:643` 圖例「現行鋁湯溫度 (TT200)」、`:642`「現行頂溫 (TT201)」，但畫的是 `df['bath_temp_c']`/`df['roof_temp_c']`（模擬值）。對現場工具而言，操作員無從分辨這是模擬還是量測 | `app_mobile.py:642-643` | 而該曲線終點 1295°C，且 `app_mobile.py:597,614` 把它當「達標」指標顯示 | ❌ **新增** |
| **L0-2** | **overhead 雙重計算**。`gas_logged_nm3` = Excel `MF本爐燃耗`（實測**總**耗氣）；calibration 拿 `simulate_trajectory` 的**淨**耗氣去擬合它 → `efficiency_scale` 已吸收 overhead；optimizer 再 `total = net + overhead` | `data_loader.py:95`；`calibration.py:90,99`；`optimizer.py:488,496` | `app.py:584` help 文字自承 overhead 五個旋鈕是為了「湊到 72 Nm³/t」反推的 → **兩套擬合機制追同一個目標值** | ❌ **新增** |
| **L0-3** | **氧化放熱化學計量超過物理上限**。`q_ox = dross_kg × 0.60 × 31050 kJ/kg`。Al₂O₃ 含 Al **52.9 wt%**，純氧化物浮渣的「已氧化 Al 質量分率」上限就是 0.529，0.60 不可能 | `optimizer.py:251`；`physics_model.py:278` | 且同一個 `cum_dross_kg` 在成本端乘 0.40（淨損失）、放熱端乘 0.60（已氧化），兩者互斥 | B2（定義混亂），**化學計量上限為新增** |
| **L0-4** | **app.py 有第二份 Sankey 實作**，硬寫 `wall_loss=250`/`regen=0.74`/`hearth=85`，與 `physics_model` 版本需人工同步 | `app.py:45-144` | 且用 `ALLOY_PROPERTIES.get(exact_key)` 而非 `get_alloy_props()` 子字串比對 → **5083A（最大宗，31% 爐次）在軌跡用 liquidus 638/cp 1.06/dross_mult 2.1，在桑基圖用 650/1.08**。5 個實際料號測試 4 個不一致 | ❌ **新增** |
| **L0-5** | **能量守恆測試是套套邏輯**。`q_net_primary := (in − absorbed)`，再由 `q_flue = q_net/(1−0.9·regen)` 反解 → `in − out ≡ 0` 是恆等式 | `physics_model.py:317-334`；`tests/test_optimizer.py:186-200` | 實跑 `residual_error_gj = 0.0`（精確）。三條斷言可代數展開證明恆真。且 `max(0.1, ...)` 在真不守恆時**靜默鉗住而非報錯** | B1（指出恆等式），**測試層面為新增** |
| **L0-6** | **`GAS_LHV=40585` 是手冊的 Gross（高位）發熱量當 LHV 用**。註解自己寫 "Gross Heating Value 9,700 kcal/Nm³"。真 LHV ≈36,500 → 燃料能量系統性高估 ~10% | `physics_model.py:29-32` | config 已改 37256，但 calibration 仍用 40585、手機版也仍用 40585（L0-1b） | B9 |
| **L0-7** | **保溫段效率釘在 clip 上界**。`combustion_efficiency(780,15) = 0.7782`，clip 上界 0.78 → 保溫（本應是最差效率工況）拿到模型的最高效率；同一值還餵給 overhead 的 `holding_gas_rate_nm3h` | `physics_model.py:186-188,424-426` | 實跑 | B4（結構），**pinned at clip 為新增實測** |
| **L0-8** | 明文密碼硬寫在原始碼 | `app.py:403` | — | ❌ 新增（非物理，順帶） |

---

### L1 結構限制（架構決定了它不可能對）

| # | 問題 | 位置 | 實跑證據 |
|---|---|---|---|
| **L1-1** | **氧化放熱的量級與帳目都錯**：(a) 100% 進入鋁液、**零份額隨煙氣離開**；(b) 氧化掉的金屬**不從 `total_weight_kg` 扣除**；(c) `q_ox_step_kj` 加在所有控制分支**之外**，湯溫達標的節流分支（`optimizer.py:191-198`）管不到它 → 正回饋失控 | `optimizer.py:251-254` | app config 下 `q_ox` = **39.9 GJ = 燃料熱的 37.6%**，隱含 ~1,285 kg 鋁完全氧化，而實測 `yield_loss_pct` 中位僅 1.12%（25% 分位精確 0，n=280）且其中含夾帶金屬。症狀：終點湯溫 **1295°C**；3.9 h 起燃氣已在下限 50 Nm³/h 湯溫仍以 ~140°C/h 上升；112/391 時步湯溫高於爐頂溫（最大 515°C）<br>⚠️ 註：表面放熱源使湯溫高於爐頂本身**不違反第二定律**（輻射項會變號讓鋁液往爐頂散熱，模型也確實處理了）。這裡的缺陷是**量級與帳目**，`bath > roof` 只是「氧化已成主熱源」的診斷症狀 |
| **L1-2** | **爐頂溫是運動學假設，不是能量平衡解**。`roof_target_step = current + clip(err×0.25, ±15°C/min)`，只有燃燒飽和分支才回饋燃料功率。爐襯蓄熱 `c_roof_eff_kj_k` 只用來算掉溫，**升溫儲能從未從燃料扣除** | `optimizer.py:168-170,188,210-219` | 這是 `efficiency_scale` 必須被擬合到 1.2353 的根因：模型的耗氣是由假設的 ε 與 h_conv 反推的，不是由燃燒物理算出來的。附帶：`c_roof_eff` 用熔池平面積 × 0.05 m 厚——火磚 6 h 的熱穿透深度 √(αt) ≈ √(4.3e-7×21600) ≈ **0.10 m**，且爐頂+爐牆面積大於熔池面積 |
| **L1-3** | **料堆是單節點集總**，65 t 廢料視為 66.15 m² 均溫平板 | `optimizer.py:102-114` | 5 t 熔湯（650°C）+ 65 t 冷料（25°C）→ **t=0 顯示湯溫 97.5°C**。爐內沒有任何東西是 97.5°C。而 `is_flat_bath = (bath ≥ liquidus)` 就靠這個假溫度切換，一切換就 dross ×2.5、輻射 ×0.70 |
| **L1-4** | **煙氣完全沒有質量/焓平衡**。`STOICH_AIR_GAS_RATIO = 9.52` 定義了但整個熱力路徑沒用到；沒有煙氣流量、沒有煙氣溫度 | `physics_model.py:33` | 因此「效率」只能是擬合曲線，不可能是推導量。桑基圖的 `q_air_preheat` 既算輸入又算輸出（同一 control volume 邊界不一致），`thermal_eff_total_pct` 分母含回收空氣 = 同一份能量計兩次 |
| **L1-5** | **燒損動力學不可識別且有假階躍**。`Ea` 固定 45 kJ/mol、`k0` 擬合 → 兩者交換，皆無物理意義；反應溫度 `0.6·T_roof + 0.4·T_bath` 為任意權重；零階速率（無氧化層成長、無 Mg 耗竭）；`is_flat_bath` 二元 ×2.5 造成液相線上的階躍 | `physics_model.py:227,229,235` | 同一層浮渣被兩組不相關常數描述兩次：`dross_factor_flat=0.70` 削減輻射、×2.5 加速氧化。方向也可疑——薄斷面廢料的比表面積遠大於平湯 |
| **L1-6** | **目標函數無內部最適 + 無上限約束**。約束只有 `final_bath ≥ target − 2`（下限），失控軌跡永遠「通過」 | `optimizer.py:383-420` | 實跑最佳解：`sp_melt=1100`(網格最小)、`t_switch=2.6h`(=0.40×deadline，最小)、`sp_soak=950`(最小)、`sp_hold=750`(最小)、`excess_air=10%`(最小) — **5/6 落在下界** |
| **L1-6b** | **整個過剩空氣搜尋網格都在現場實際操作點之外**（安全項） | `optimizer.py:392` | 用模型自己的 `calculate_flue_oxygen_pct`：網格 `[10, 12.5, 15]%` → flue O₂ **1.90 / 2.32 / 2.72%**，全部低於廠內 O₂ trim 預設 3%。反推 O₂=3% 需要 **EA=16.8%**，已高於手冊高火設計上限 15%；O₂=1% 需要 EA=5%。而實測空燃比推得 EA ≈ **49–57%**（O₂ 6.8–7.5%）。**模型建議的操作點與現場實際差一個數量級，且無 CO/未燃/火焰穩定懲罰項**<br>⚠️ 設下界時須與 AT104 的乾/濕基一致，否則約束會偏 10–15% 相對值 |
| **L1-7** | **soak 段長度寫死 1.2 h**，不在搜尋空間內；`sp_soak`/`sp_hold`/`excess_air` 候選集全部硬編碼 | `optimizer.py:390-397` | 使用者只能調對照組，不能調搜尋空間 |

---

### L2 未驗證主張（缺資料或缺實驗設計）

| # | 問題 | 說明 |
|---|---|---|
| **L2-1** | **可識別性：對「排程」的響應完全未驗證** | 123 爐全部用同一組假設 SP（1100/1020/25% EA）重播校正，因為「真實歷史 SP 未被記錄」（`calibration.py:9-20`）。**擬合出的常數不帶任何關於「改變排程會怎樣」的資訊** — 而排程正是 app 唯一的決策變數。措辭要精確：不是「17.55% MAPE > 節省幅度所以節省是雜訊」（配對比較可以解析小於絕對誤差的差異），而是**絕對準度 17.55% MAPE，而排程的微分響應未經測試，且校正設計使其無法被測試** |
| **L2-2** | **17.55% MAPE 是 in-sample 擬合殘差，非 hold-out** | `evaluator.py` 只算「省了多少錢」，沒有 MAPE/RMSE/R²（`evaluator.py:162-172`）。全 repo 沒有 hold-out 驗證 |
| **L2-3** | **有 5 秒級實測湯溫（tt200）與爐頂溫（tt201），完全沒進入任何 residual** | `data_loader.py:253-255` 產出 `peak_roof_temp_c`/`max_bath_temp_c` 後，`evaluator._backtest()` 只讀重量/時長/耗氣/合金。**這是目前最大的浪費**：軌跡模型有軌跡真值卻不用 |
| **L2-4** | **`ft211`/`ft215` 是否真的是燃氣流量，未經現場 I/O 清單確認**；`ft213`/`ft217` 兩支流量計未識別、未使用 | `MELTER_KNOWLEDGE.md:179-184`；`data_loader.py:28-29`。若判讀錯誤，SCADA 側的 68.7 Nm³/t、「55% 燃料無去處」等全部推論失效。**這是地基，必須先裁決** |
| **L2-5** | **實廠能量帳本身不閉合**：燃料 180.4 GJ，金屬吸熱 36%、煙囪顯熱僅 4–7%，約 **55%（~102 GJ）無量測去處** | `PHYSICS_AUDIT:104-134`。三個候選解釋（冷風滲入 / 燃耗帳基準 / tt11x 是否主煙道）待現場裁決 |
| **L2-6** | **蓄熱床實測資料未回饋**。`regenerator_model.py`(348 行) 是孤立模組，只有測試 import 它 | `regen_base_eff = 0.74` 是硬編碼常數，而 tt111–114 + tt201 可直接量出有效度（`PHYSICS_AUDIT:130` 推得 ≈0.89）。**現成的量測約束被丟掉** |
| **L2-7** | **廢料潔淨度未入模** | 生產紀錄有水力鋁/PCR/PIR/SPR/OSR 五類配比（`MELTER_KNOWLEDGE.md:252-270`），而燒損最大的物理驅動之一就是廢料形態與污染度。模型完全無此輸入 |
| **L2-8** | **燒損目標函數近乎不可證偽** | `yield_loss_pct` 25% 分位 = 精確 0（n=280），std 4.46 與校正 MAE 3.52 同量級 |

---

### 全 repo 常數沒有單一真相來源（橫向問題）

| 常數 | repo 內同時存在的值 |
|---|---|
| `GAS_LHV` | 36000（文件）/ 40585（code + calibration + **手機版實際**）/ 37256（config + **桌面版實際**）/ ~36500（真 LHV） |
| `HEARTH_AREA_M2` | 45.0（文件）/ 66.15（code + 手冊） |
| `max_gas_flow_dual_pair` | 1300（code + calibration）/ 880（config + app）/ 850–880（實測峰值）/ 1418（銘牌）/ 1700（design） |
| `regen_eff` | 0.70（註解）/ 0.74（config）/ ≈0.89（實測推得） |
| `excess_air` | 15（手冊高火上限）/ 25（校正假設）/ 40（config 預設）/ 10–15（最佳化網格）/ 49–57（實測） |
| `emissivity_eff` | 0.45（校正）/ 0.85（config + app） |

---

## 第二部分：修訂方案

> **貫穿原則：減少自由參數，增加量測約束。**
> 不要新增沒有資料能約束的物理（氣體參與性輻射、Mg 耗竭動力學、第一原理蓄熱床）——每一項都增加自由參數，讓本來就很差的可識別性更差。模型已經必須把 `efficiency_scale` 硬拉到 1.24 去吸收結構誤差。正確方向是把「效率」從**自由參數**變成**推導量**，再用現成的 SCADA 量測去釘住它。

### 本輪範圍（依使用者裁決）

| 項目 | 裁決 |
|---|---|
| **用途** | **現場操作指導工具** → 排序準則是「錯的建議會不會害到現場」，其次才是準度 |
| **本輪做到** | **P0 止血**（不動架構）。修完重跑，用實際數字決定要不要往 P1 走 |
| **P-1 地基確認** | 使用者自行向現場索取 MFA I/O 清單 |
| **P1 / P2** | 列為後續，本輪不做 |

### P-1｜地基確認（使用者負責，與 P0 併行）

| 要確認 | 影響 |
|---|---|
| `ft211/ft215` 是否真為燃氣、`ft213/ft217` 是什麼、`ft210/ft214` 是否為助燃空氣 | 若 ft213/217 也是燃氣 → `gas_integrated_nm3` 低估，evaluator 的 ground truth 需重算，實測 68.7 Nm³/t 基準要改 |
| `tt111–114` 實際安裝位置（蓄熱介質 / 冷端排氣 / 預熱空氣出口） | 決定 P1-2 能否用它量蓄熱有效度。目前 `REGENERATIVE_SYSTEM_ANALYSIS` 與 `PHYSICS_AUDIT` 對此判讀相反 |
| `ot104`（O₂ 分析儀）為何全週恆為 0 | 決定過剩空氣能否有真值。這是 P0-2 安全下界唯一可能的量測依據 |
| 現場實際的 O₂ trim 設定值與過剩空氣操作範圍（含乾/濕基） | 直接決定 P0-2 的下界該設多少 |

> 在 P-1 回覆之前，P0 照做——P0 的 8 項全部不依賴這些答案。

### P0｜止血（不動架構，~1 天）

排序準則：**現場安全 → 建議正確性 → 帳目一致性 → 工程衛生**。
**依序執行，不要平行做** — 見下方「執行順序的三個相依」。

| # | 對應 | 動作 | 驗收 |
|---|---|---|---|
| **P0-1**<br>🔴安全 | L1-1 | **氧化放熱三道護欄**：(a) `q_ox` 只有一部分進入鋁液，其餘隨煙氣離開（比例可調、預設保守）；(b) `q_ox` 納入湯溫達標的節流分支——現在它加在所有控制分支**之外**；(c) 修完後若湯溫仍高於爐頂，用 **assert 曝露、不要 clamp**。<br>⚠️ 不可用硬鉗 `T_bath = min(T_bath, T_roof)`——那正是 L0-5 批評的「靜默鉗住而非報錯」，會把 bug 藏起來讓 V2 失去意義 | 全時步 `bath_temp ≤ roof_temp`（目前 112/391 違反，最大高 515°C）；終點湯溫在服役範圍內（目前 1295°C）；`q_ox` 佔燃料熱比例降到與實測燒損量級相容 |
| **P0-2**<br>🔴安全 | L1-6b | **加過剩空氣下界與湯溫上限硬約束**。現況最佳解恆選 EA=10% → flue O₂ **1.90%**，且整個網格 `[10,12.5,15]` → O₂ 1.90–2.72% 全部低於廠內 trim 預設 3%；實測操作點在 EA 49–57%。**現場照這個配方調空燃比會有不完全燃燒風險。** 下界暫以 O₂=3% 反推（=EA 16.8%），待 P-1 回覆後改用實際操作範圍；須確認乾/濕基與 AT104 一致。湯溫上限用手冊服役範圍 690–750°C | 建議配方的 flue O₂ 落在廠內 O₂ trim 可接受範圍內 |
| **P0-3** | L0-1, L0-1b, L0-1c | **統一三條建構路徑**（`app.py` / `app_mobile.py` / `calibration.py`）走同一個 loader，`config/furnace_parameters.json` 為唯一入口。修 `app_mobile.py:232` 的 `gas_lhv_kj_nm3=` → `gas_lhv=`；**移除 `physics_model.py:114` 的 `**kwargs`**（它是這個錯誤能靜默通過的原因）。`app_mobile.py:642-643` 的圖例移除 `TT200`/`TT201` 位號或明確標示「模擬值」 | 新增測試：以 **app.py 與 app_mobile.py 的實際建構路徑**（非裸建構）斷言 ε/LHV/流量上限三者彼此相同、且 == calibration 建模時的值 |
| **P0-4** | L0-2 | **解決 overhead 雙計**：建議校正對象改為 `net + overhead` 的總量，並把 overhead 五個旋鈕從「擬合旋鈕」降級為固定工程假設（`app.py:584` 自承是為湊 72 Nm³/t 反推的） | 校正殘差的量 == UI 顯示的總耗氣的量 |
| **P0-5** | L0-3 | **統一 `cum_dross_kg` 的質量定義**：建議定義為「淨消失金屬」，與 `data_loader.compute_heat_yield_loss` 一致，成本（0.40）與放熱（0.60）都由此推導而非各用一套。放熱係數上限 ≤0.529（Al₂O₃ 含 Al 52.9 wt%）。氧化生成的 Al₂O₃ 質量從 `total_weight_kg` 扣除 | 新增**質量守恆測試**：`裝料 + 殘湯 = 出湯 + 燒損 + 爐內殘留`；`cum_dross_kg` 不得超過金屬總量 |
| **P0-6** | L0-4 | 刪除 `app.py:45-144` 的重複 Sankey，改呼叫 `physics_model.calculate_sankey_energy_balance`；全 repo 合金解析統一走 `get_alloy_props()` | 新增測試：軌跡與桑基圖對 `5083A`/`5052KS`/`5182E`/`6061H` 解析出同一組合金性質 |
| **P0-7** | L1-6, L1-7 | **搜尋空間改為可配置並納入搜尋**：`sp_soak`/`sp_hold`/`excess_air` 候選集與 soak 段長度（現寫死 1.2 h）。若最佳解仍落在邊界，**UI 必須明示「解在搜尋邊界上」**。<br>⚠️ **先做完 P0-1 再決定這項的範圍**（見相依 ①） | 最佳解不再 5/6 落在下界，或已明示 |
| **P0-8** | L0-5, L2-2 | 刪除套套邏輯測試，改為對**外部量測**的殘差測試；同時移除 `test_sensitivity.py:66` 硬編碼的 `cum_dross_kg ≈ 1743.1`（重擬合必定讓它爆掉，見相依 ②）。**現場工具版 UI 要求**：`thermal_eff_fuel_pct`（實跑 77.6% vs 實測 ~36%）與節省金額必須帶不確定區間並標註「in-sample MAPE 17.55%」——操作員看到單點數字會當真值 | 故意改壞一個常數，守恆測試要變紅；UI 無單點無區間的數字 |

#### 執行順序的三個相依

| ① | **P0-1 可能自己就解掉 P0-7 大半。** 目前網格解全落在下界，是因為 `q_ox` 讓湯溫無論設點多低都會達標 → `final_bath ≥ target−2` 在每個網格最小值都成立，兩個目標又同向遞減。修掉氧化項後，低爐頂設點會開始**達不到時限**，內部取捨才會出現。**先做 P0-1、重跑、再決定 P0-7 的範圍**（可能縮小到「把硬編碼候選集放寬」而已） |
| ② | **P0-3 與 P0-4 都要求重新校正。** 明訂 **P0 只跑一次重校正**，在 P0-1～P0-6 全部改完後執行，輸入固定為：ε / LHV / 流量上限皆取自 config，擬合目標 = `net + overhead` 對 `MF本爐燃耗`。否則兩項會各觸發一次、且假設不同 |
| ③ | **重校正跑不動在這個 worktree。** `115年 MFX生產紀錄.xlsx` 與 SCADA CSV 被 `.gitignore` 排除，實體檔在主 repo 根目錄，**本 worktree 內不存在**。動工前需先把資料檔複製進來或改走絕對路徑，否則「P0 約 1 天」的估算不成立 |

### P1｜結構重寫：讓「效率」變成推導量（後續，1–2 週）

| # | 對應 | 動作 |
|---|---|---|
| P1-1 | L1-4 | **建立煙氣質量/焓路徑**：用已定義未用的 `STOICH_AIR_GAS_RATIO` 由燃氣流量與過剩空氣算出煙氣流量；`available heat = 1 − (煙氣焓 @T_exit − 預熱空氣焓 @T_preheat)/LHV`。`combustion_efficiency()` 的線性擬合與 `efficiency_scale` 一併刪除 |
| P1-2 | L2-6 | 蓄熱有效度改由 `tt201`（爐膛出口）與 `tt11x`（冷端）量測推導，取代硬編碼 0.74。把 `regenerator_model.py` 從孤島接回熱平衡 |
| P1-3 | L1-2 | 爐膛加一個能量平衡節點（爐氣 + 爐襯熱容），解出 `T_roof` 而非用一階遲滯假設它。爐襯升溫儲能必須從燃料扣除；有效厚度用熱穿透深度（6 h 約 0.10 m）而非 0.05 m，面積用爐頂+爐牆而非熔池平面積 |
| P1-4 | L1-3 | 料堆至少二節點（固態料堆 / 液相 heel），或改用熔化前緣模型。`is_flat_bath` 改由液相分率門檻決定，取代二元切換 |
| P1-5 | L1-5 | 燒損：`Ea` 與 `k0` 不可同時存在於一個一維擬合 → 要嘛用文獻 `Ea` 做敏感度帶、要嘛設計能分離兩者的資料。移除 ×2.5 階躍，改為隨液相分率連續變化。把廢料潔淨度（P2-6）納為輸入 |

### P2｜用現成資料重新校正與驗證（後續，與 P1 併行）

| # | 動作 | 為什麼現在做得到 |
|---|---|---|
| P2-1 | **把 tt201/tt200 軌跡納入校正 residual**（不只總耗氣）：模型必須同時對得上耗氣**和**溫度軌跡 | 5 秒級真值已在 `data_loader` 產出，只是沒接下游 |
| P2-2 | **保溫段直接量 `wall_loss_kw`**：保溫穩態下 `SCADA 燃氣流量 × 效率 = wall + hearth loss` | 現在 250 kW 是固定假設值（`calibration.py:67-72` 自承不可識別） |
| P2-3 | **TT201 對燃燒率階躍的響應辨識** `k_roof_response` 與增益 | 目前 τ≈3.5 min 是憑空設定 |
| P2-4 | **可識別性實驗（解 L2-1）**：用 **TT201 實測軌跡**（不是未記錄的 SP）把 123 爐分群，測模型能否重現**群間**的耗氣差異 | 達成軌跡的資訊比 SP 更好，而且它有紀錄 |
| P2-5 | **改 hold-out 驗證**，`evaluator` 加 MAPE/RMSE/R²；MAPE 傳遞到 UI，節省金額必須帶區間 | 目前 17.55% 是 in-sample |
| P2-6 | 廢料潔淨度（水力鋁/PCR/PIR/SPR/OSR 配比）納入燒損模型輸入 | 生產紀錄已有此欄位 |

> `test_sensitivity.py:66` 硬編碼期望值 `1743.1` 已移至 **P0-8**（P0 的重校正必定讓它爆掉）。

### 關鍵檔案

| 檔案 | 涉及 |
|---|---|
| `src/physics_model.py` | L0-3/5/6/7、L1-4/5；P0-1/3/5/6、P1-1/5（**`**kwargs` 需移除**） |
| `src/optimizer.py` | L1-1/2/3/6/7；P0-1/2/7、P1-3/4 |
| `src/calibration.py` | L0-1/2、L2-1/2；P0-3/4、P2-1/4（**目前零測試覆蓋**） |
| `app.py` | L0-1/4/8；P0-3/6/8 |
| `app_mobile.py` (751 行) | **L0-1b/1c**；P0-3。第三條獨立建構路徑，LHV 參數名拼錯被 `**kwargs` 吞掉，且把模擬曲線標成 TT200/TT201。**現場工具很可能主要用這一版** |
| `src/evaluator.py` | L2-2/3；P2-5 |
| `src/regenerator_model.py` | L2-6；P1-2（現為孤島） |
| `config/furnace_parameters.json` + `src/calibrated_constants.json` | L0-1；P0-3（合併為單一真相來源） |
| `tests/` | L0-5；P0-3/5/6/8（缺 `test_physics_model.py` / `test_calibration.py`） |

---

## 驗證方式（端到端，不需新增儀器）

| # | 檢查 | 通過條件 | 本輪(P0) |
|---|---|---|---|
| V1 | 建議配方的 flue O₂ 落在廠內 O₂ trim 可接受範圍 | 目前網格 `[10,12.5,15]%` → O₂ 1.90/2.32/2.72%，全部低於廠內 trim 預設 3% | ✅ |
| V2 | 全時步 `bath_temp ≤ roof_temp`（**用 assert 不用 clamp**）；終點湯溫在服役範圍；`q_ox` 佔燃料熱比例與實測燒損量級相容 | 目前失敗（112/391 時步、最大 515°C；終點 1295°C；`q_ox` 佔 37.6%） | ✅ |
| V3 | 最佳解不落在搜尋網格邊界，或 UI 明示 | 目前 5/6 落在下界 | ✅ |
| V4 | 以 **app.py 與 app_mobile.py 各自的實際建構路徑**跑一次，印出 ε/LHV/流量上限/`efficiency_scale` | 三條路徑（含 calibration）完全相同。目前 mobile LHV=40585、desktop=37256，差 8.94% | ✅ |
| V5 | 質量守恆：`裝料 + 殘湯 − 出湯 − 燒損 = 爐內殘留` | 目前無此測試 | ✅ |
| V6 | 守恆測試改為對外部量測的殘差；故意改壞一個常數要能讓它變紅 | 目前 `residual_error_gj ≡ 0` 恆真 | ✅ |
| V7 | 模擬單耗落在實測分布內 | 實測中位 70.0 Nm³/t；目前 baseline 53.7、最佳解 46.4。**P0 不會達標**（見風險），但要記錄修正後的新值 | 記錄 |
| V8 | 40 爐 hold-out MAPE；保溫段 SCADA 反算 `wall_loss_kw` | 目前無 hold-out（in-sample 17.55%）；250 kW 為未量測假設 | P2 |

執行方式：
- V1–V4、V7 用 `python -c` 直接 import `src.optimizer` / `src.physics_model` 跑（唯讀，不寫檔），V5/V6 走 `pytest tests/`。
- ⚠️ 不要用 `python -m src.regenerator_model` 做驗證——它的 `__main__` 會覆寫 `regenerator_baseline.json`。
- ⚠️ **V7 與重校正需要資料檔**：`115年 MFX生產紀錄.xlsx` 與 SCADA CSV 被 `.gitignore` 排除、本 worktree 內不存在，需先從主 repo 根目錄取得。

---

## 已知風險

| 風險 | 說明 |
|---|---|
| **P0 做完數字會更難看，這是預期的** | 修掉 L1-1（氧化失控）後單耗降到 ~46.6 Nm³/t，離實測 70.0 還差 33%。**修正 ≠ 變準** — P0 是移除錯誤，不是補上缺失的物理。要收斂到 70 Nm³/t 必須做 P1 的煙氣質能路徑與損失預算。P0 之後請以「錯誤是否消失」而非「數字是否變準」判斷成敗 |
| **節省幅度會大幅縮水** | 目前 41.65% / NT$52,214 有相當比例來自兩條失控軌跡的差（最佳解終點 956°C vs baseline 1291°C，兩者皆不合法）。P0 之後的真實幅度可能小很多，甚至可能為負。若這個數字已對外承諾過，需提前準備說法 |
| **現場工具定位下的額外要求** | 在 P1 完成前，app 給出的配方只能當「相對比較」參考，不能當絕對耗氣預測。UI 必須明示這個限制，否則操作員照著單點數字調爐 |
| P-1 若判讀翻盤 | `ft211` 若不是燃氣，所有能耗基準（68.7 / 70.0 Nm³/t）與「55% 燃料無去處」推論需重做 |
| 平行 worktree | `PHYSICS_AUDIT:7` 記載另有 worktree `melter2-physics-revision`（branch `physics-model-revision`）在改同一批檔案。**動工前必須先確認它的狀態**，否則會撞車 |

---

## 附錄：本輪相對 `PHYSICS_AUDIT_2026-08-20.md` 的增量

| 既有 audit 項 | 本輪處置 |
|---|---|
| A（氧化正回饋） | 複驗**未修**；量化為「佔燃料熱 37.6%、隱含 1,285 kg 鋁氧化 vs 實測燒損中位 1.12%」；**修正**原 audit 的「違反第二定律」框架 → 應為量級與帳目錯誤 |
| B1（Sankey 代數閉合） | 複驗**未修**；**新增**：`tests/test_optimizer.py:186-200` 三條斷言皆為套套邏輯，是一個永不會失敗的「守恆測試」 |
| B2（dross 定義混亂） | 複驗**未修**；**新增**：0.60 超過 Al₂O₃ 含 Al 的物理上限 52.9 wt% |
| B3（執行期≠校正期常數） | 複驗**未修**；**新增**：漏了 `max_gas_flow`（880 vs 1300）；**新增** `app_mobile.py` 第三條路徑與 `gas_lhv_kj_nm3` 拼錯被 `**kwargs` 吞掉 |
| B4（效率非焓平衡） | 複驗**未修**；**新增**實測：保溫段 `combustion_efficiency(780,15)=0.7782` 釘在 clip 上界 0.78 |
| B5（過剩空氣角解） | 複驗**未修**；**新增**：整個網格 → O₂ 1.90–2.72%，全低於廠內 trim 預設 3%；O₂=3% 需 EA=16.8%（高於手冊 15% 上限）；實測 EA 49–57% |
| B6（浮渣雙重描述） | 複驗**未修** |
| B7（集總單節點） | 複驗**未修**；**新增**實測：5 t 熔湯 + 65 t 冷料 → t=0 顯示湯溫 **97.5°C** |
| B8（爐頂溫為運動學） | 複驗**部分處理**；**新增**：火磚 6 h 熱穿透深度 ≈0.10 m vs 模型 0.05 m，且用熔池平面積 |
| B9（Gross 當 LHV） | 複驗**未修**；**新增**：手機版仍跑 40585 |
| — | **全新**：L0-2 overhead 雙重計算（calibration 對 net、app 報 total） |
| — | **全新**：L0-4 `app.py:45-144` 重複 Sankey + 合金解析 exact-key（5083A 最大宗料號不一致） |
| — | **全新**：L0-1b/1c 手機版三項問題 |
| — | **全新**：L2-3 tt200/tt201 有 5 秒級真值卻不進 residual |
| — | **全新**：實跑證實最佳解 5/6 落在網格下界 |
