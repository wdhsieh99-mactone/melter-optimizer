# 80T 鋁熔爐模型物理審查與修訂方案

審查基準：commit `612af99` (v2.3.1)，2026-08-20
驗證方法：逐行讀碼 + 對 `612af99` 的**釘選**執行實測（`scratch_probe.py`，本文所有數字皆可重跑複現）+ 對照 318 爐生產紀錄與 20 爐 SCADA 5 秒資料
本文取代 `PHYSICS_MODEL_REVIEW.md`（同日較早版本），關係見文末附錄 A

> **並行修改警告**：審查期間主 checkout 的 `src/optimizer.py` 有未提交修改（16:15），內容部分觸及本文錯誤 A。詳見附錄 B。本文所有數字釘選於 `612af99`，不含該未提交 patch。

---

## 結論先行

現行模型有**兩個彼此獨立**的問題，不能用同一個修法解決：

| | 問題 | 現況 | 應為 |
|---|------|------|------|
| **A** | 氧化放熱項無熱匯、無質量扣除、無溫度上限 → **正回饋失控** | 65 t 爐次模擬終點湯溫 **1319 °C**，燒嘴自 3.5 h 起已在流量下限 50 Nm³/h，湯溫仍持續上升 | 實測終點湯溫 709–792 °C |
| **B** | 損失預算短缺 ~4.7 MW（與 A 無關，修掉 A 也不會改善） | 模型 46.6 Nm³/t | 實測中位數 **70.0** Nm³/t（n=318）、SCADA 20 爐 65–92 Nm³/t |

A 是**單一根因**，它污染了介面上所有下游數字（Sankey 熱效率 88%、基準燒損 2526 kg、「節省 42.5%」）——這些不是各自獨立的 bug，是同一個缺陷的顯影。
B 是**結構性缺口**：模型全部已建模損失只有 335 kW，實際未解釋損失是 4,738 kW。

順帶結論：**燒損那一半的目標函數目前無法被這份資料證偽**（見 §4），所以「降頂溫抑燒損」的效益主張在現有驗證框架下站不住。而修訂順序必須是「先修守恆 → 再修損失 → 最後才重新校正常數」——`c32ae8a` 已經試過先重新校正並被 revert，原因見 §5.0。

---

## 1. 錯誤 A：氧化放熱失控（根因）

### 1.1 缺陷本體

`src/optimizer.py:251`

```python
q_ox_step_kj = (dross_step_kg * 0.60 * 31050.0)
...
current_energy_kj += (q_step_kj + q_ox_step_kj)      # optimizer.py:254
```

化學計量本身**沒有錯**：Al₂O₃ 生成焓 −1675.7 kJ/mol ÷ 2 Al ÷ 0.027 kg/mol = 31.03 MJ/kg-Al。錯在三個守恆環節同時缺失：

| # | 缺失 | 後果 |
|---|------|------|
| 1 | **沒有熱匯**：氧化發生在熔池表面／渣層，放熱主要隨爐氣排走並提高煙氣溫度；模型 100% 記入鋁液焓 | 憑空給熔池灌入 47% 燃料當量的熱 |
| 2 | **沒有質量扣除**：`total_weight_kg` 在 `optimizer.py:102` 之後不再更新 | 被燒掉的金屬同時「消失」又「繼續被加熱」，質量與能量兩邊都不守恆 |
| 3 | **沒有溫度上限／回饋阻尼**：湯溫↑ → `physics_model.py:227` 的 Arrhenius 表面溫度↑ → 燒損率↑ → 放熱↑ → 湯溫↑ | 純正回饋，無自然平衡點 |

### 1.2 實測驗證（釘選 `612af99`，replay policy 1100 °C melt / 1020 °C hold / 25% EA / 5 t 殘湯）

```
  55.0t -> Tbath_end=1596.6C  dross=3264kg  gas=41.5 Nm3/t  q_ox=60.8GJ (66% of q_fuel)
  60.0t -> Tbath_end=1438.1C  dross=2941kg  gas=41.0 Nm3/t  q_ox=54.8GJ (55%)
  65.0t -> Tbath_end=1318.9C  dross=2686kg  gas=40.6 Nm3/t  q_ox=50.0GJ (47%)
  70.0t -> Tbath_end=1226.1C  dross=2476kg  gas=40.2 Nm3/t  q_ox=46.1GJ (40%)
```

65 t 爐次軌跡：

| t (h) | 頂溫 | 湯溫 | 燃氣流量 | 累積燒損 |
|---|---|---|---|---|
| 1.0 | 1100 | 463 | 910 | 75 |
| 3.0 | 1100 | 743 | 477 | 462 |
| 4.0 | 1100 | **892** | **50**（下限） | 968 |
| 5.0 | 1100 | **1043** | **50** | 1576 |
| 6.0 | 1039 | **1225** | **50** | 2306 |

**燒嘴已經關到不能再關，湯溫仍以 ~150 °C/h 上升。** 熱源只剩氧化放熱。

三個關鍵旁證：
- **熱質量越小越嚴重**（55 t → 1597 °C），符合「面積比例熱源 ÷ 質量」的正回饋特徵，不是數值誤差。
- **兩個護欄都沒有觸發**：質量守恆護欄 20%（`optimizer.py:245`）在 2686 kg = 3.8% 未觸發；速率上限 2167 kg/h（`physics_model.py:61`）在峰值 ~480 kg/h 未觸發。**「爐次時間到了」是唯一的終止機制。**
- **反事實**：把燒損率強制設為 0 後，終點湯溫 **790.1 °C**（合理）。

### 1.3 級聯污染清單（不是獨立問題，修好 A 即消失）

| 介面顯示的數字 | 目前值 | 為何無效 |
|---|---|---|
| Sankey `thermal_eff_fuel` | **88.0%**（baseline） | `q_al_absorbed` 是以終點湯溫 1296 °C 計算的焓 |
| 基準爐次燒損 | 2526 kg (3.9%) | 失控高溫下的 Arrhenius 外插 |
| 「最佳 vs 傳統」節省 | **42.5%**（NT$54,179/爐） | 其中 1594 kg 燒損差（≈75% 的節省金額）來自兩條軌跡失控程度不同 |
| 最佳解終點湯溫 | 950 °C | 目標是 780 °C；最佳化「達標」判定因此失真 |
| Sankey 預熱空氣 | 42–53 GJ | 由被污染的燃氣總量推導 |

> **判讀**：目前 UI 呈現的節省金額，其主要成分是兩條失控軌跡的差，不是可實現的操作改善。

---

## 2. 錯誤 B 群：獨立結構性缺陷（修好 A 之後依然存在）

| # | 位置 | 缺陷 | 證據 |
|---|------|------|------|
| **B1** | `physics_model.py:324` | **Sankey 是代數閉合，不是熱力學平衡**：`q_flue = q_net/(1−0.9·regen_eff)` 沒有任何溫度約束，`residual_error_gj` 恆為 0 是恆等式的結果，不是驗證 | 推得預熱空氣 ΔT = 850 °C（baseline）／**994 °C（最佳解）**；但最佳解的平湯段頂溫只有 950 °C ⇒ **預熱空氣比加熱它的煙氣還熱**，違反第二定律。另 `thermal_eff_total_pct`（:337）分母含回收空氣，等於把同一份能量計入兩次 |
| **B2** | `physics_model.py:242` vs `:278` vs `calibration.py:124` | **`cum_dross_kg` 一個量、三種互斥定義**：校正對象是 `yield_loss_kg`＝淨消失金屬（`data_loader.py:133`）；成本端乘 **0.40** 回收折減；放熱端乘 **0.60** 已氧化比例 | 若 60% 已氧化則永久損失 ≥60%，不可能同時是 40%；若它是淨消失金屬則兩個係數都不該存在。經濟損失目前**少算 2.5×** |
| **B3** | `app.py:344-380` vs `calibrated_constants.json` | **執行期常數 ≠ 校正期常數**。config：ε=0.85、LHV=37256；校正時：ε=0.45、LHV=40585。而 `efficiency_scale=1.2353` **從未寫進 config**，靜默沿用校正值 | 輻射（主導項）差 **1.89×**。校正出來的 `efficiency_scale`/`burnoff_k0` 對 UI 實際跑的那組物理是無效的 |
| **B4** | `physics_model.py:179-188` | **`combustion_efficiency()` 是溫度線性擬合而非煙氣焓平衡**；且 clamp 在乘 `efficiency_scale` **之後**才做 | EA=10% 時，roof 750/780 °C 一律撞 0.78 上限；optimizer 的 `sp_roof_hold∈{750,780}` 全落在飽和區 ⇒ 該決策變數在效率通道上無區分度 |
| **B5** | `optimizer.py:392` | **過剩空氣是無界角解**：唯一懲罰是效率線性項與 O₂ 燒損項，沒有 CO／未燃／火焰穩定下界 | 網格 `[10, 12.5, 15, 40]` → 最佳解恆取 **10%**（網格最小值）。實際再生式燒嘴低於 ~10–15% 過剩空氣會出 CO 與積碳 |
| **B6** | `physics_model.py:227,229,235` | **燒損動力學的溫度敏感度結構性偏弱**——這正好稀釋了本專案的核心主張 | ① Arrhenius 溫度用 `0.6·T_roof+0.4·T_bath`：頂溫 1180／湯溫 730 → 推得「鋁液表面 1000 °C」，鋁液導熱率 ~90 W/m·K 不可能有 270 °C 的表面過熱；② Ea=45 kJ/mol 從未校正 → 頂溫 1050→1250 °C 只有 **1.51×**（Ea=120 kJ/mol 時 3.0×，200 kJ/mol 時 6.3×）；③ `is_flat_bath` 二元 2.5× 開關在液相線造成 **158→395 kg/h 躍階**，最佳化網格會在此不連續處產生假極值 |
| **B7** | `optimizer.py:106-114` | **集總單節點 + 用焓平均溫度算 T⁴**。固體料堆期的真實限制是向料堆內部的導熱與接觸熱阻；模型把 65 t 廢料當成一塊 66.15 m² 均溫平板 | 平均溫度低估表面溫度 ⇒ **高估** ΔT⁴ ⇒ 高估前段熱傳；料堆／平湯的幾何差異只用一個 0.7 輻射因子表示 |
| **B8** | `optimizer.py:169-170, 188` | **頂溫是運動學而非能量平衡解出**：`roof_err×0.25`、上限 15 °C/min，只有燒嘴飽和分支才回饋燃料。爐襯蓄熱 `c_roof_eff_kj_k`（7,640 kJ/K）只用來算掉溫，**升溫時儲存的能量從未從燃料扣除** | 850→1180 °C 約 2.5 GJ 未計；且只算爐頂 5 cm，牆體與蓄熱體完全不在動態平衡中（被 `refractory_reheat_gj=7.0` 這個常數硬編碼取代） |

---

## 3. 未考慮但重要的項目：缺失的 4.7 MW

這是與錯誤 A **完全獨立**的第二個工作面。修掉 A 只把 40.6 → 46.6 Nm³/t，離實際 70.0 還差 23 Nm³/t。

**損失預算對帳（65 t + 5 t 殘湯 → 780 °C，6.5 h）**

| 項目 | GJ | 平均功率 |
|---|---|---|
| 實際燃料投入（65 t × 70 Nm³/t） | 184.7 | 7,893 kW |
| 金屬理論焓（含潛熱與過熱） | 73.8 | 3,154 kW |
| **未解釋** | **110.9** | **4,738 kW** |
| 模型全部已建模損失（wall 250 + hearth 85） | 7.8 | **335 kW** |

模型只解釋了缺口的 **7%**。以下是這 4.7 MW 可能的去處，目前**全部不在動態軌跡裡**（部分只以事後 gas adder 形式加在成本上，不影響溫度與燒損）：

| 未建模項 | 目前狀態 | 為何重要 |
|---|---|---|
| **開門加料輻射 + 冷風滲入** | 事後 `calculate_field_overhead_losses` 加 Nm³，不進軌跡 | 7 m² 門口 1000 °C 輻射即 ~1 MW；冷風滲入的顯熱損失完全未計 |
| **爐壓／氣密洩漏** | 完全沒有 | 反射爐長期是主要隱性損失；`app.py` 也無爐壓量 |
| **保溫等待鑄機** | `gas_holding_extended_nm3 = 超時 × 60 Nm³/h`（硬編碼常數） | SCADA 顯示實際爐次 5.3–13.7 h，等待段長度差異極大 |
| **換向 purge** | `reversal_loss_pct=4.0` 假設值 | `regenerator_baseline.json` 顯示換向週期實測 240 s 固定（std 3.9 s），可直接算，不必假設 |
| **爐襯／蓄熱體蓄熱與週期冷啟** | 僅 `refractory_reheat_gj=7.0` 常數 | 見 B8 |
| **煙氣顯熱** | 從未在前向模擬出現，被 `efficiency` 一個係數吸收 | 這是能量平衡的主項，不該是擬合係數 |
| **廢料含水／含油／料型** | 完全沒有 | 影響熱傳與燒損；MFA 紀錄有料型欄位可用 |

**一個可以用既有資料立刻裁決的分岔點**：`regenerator_baseline.json` 的 tt111–tt114 平均位準只有 **121–151 °C**。若這確實是蓄熱室冷端（排煙側）溫度，則再生器回收率遠高於模型假設的 0.74，代表這 4.7 MW **不在煙囪，而集中在爐體開口與洩漏**——這會完全改變改善方向（該做的是爐門管理與氣密，不是調頂溫曲線）。這件事應該在動任何模型之前先確認。

---

## 4. 燒損目標函數目前不可證偽

`compute_heat_yield_loss` 產出的 `yield_loss_pct`（n=280）：

| 統計量 | 值 |
|---|---|
| 25% 分位 | **0.00（精確 0）** |
| 中位數 | 1.12 |
| 平均 | 3.40 |
| 標準差 | 4.46 |

四分之一的爐次「金屬損失恰好為零」——這是**帳面平衡的結果，不是量測**。校正報告的 `dross_mae_pct_points=3.52` 與這個目標的 std 4.46 同量級。

**結論**：整個最佳化目標函數裡「多燒一點氣、少燒一點鋁」的取捨，**無法用這份資料驗證方向是否正確**，更不用說量級。這比「MAE 偏大」嚴重——它意味著推薦曲線的核心效益主張目前是不可證偽的。

**校正資料的正確來源應該是 SCADA 週資料，不是生產紀錄總量。** SCADA 有 TT201（實際頂溫軌跡）、TT200（實際湯溫）、FT211+FT215（實際燃燒率）共 20 爐 5 秒資料 —— 這直接消解了 `calibration.py` 開頭那句「我們不知道實際 SP 排程」的 caveat。每爐燃氣總量是 1 維純量，本質上無法辨識一個動態模型；加了損失項之後只會讓 `wall_loss_kw` 與 `efficiency_scale` **更不可辨識**，不是更準。

---

## 5. 修訂方案

### 5.0 先講為什麼不能從「重新校正」開始

`c32ae8a`（已被 `90a9d43` revert）做的正是「用 123 爐重新校正」：

| 參數 | 校正前 | c32ae8a 校正後 |
|---|---|---|
| `efficiency_scale` | 1.2353 | **0.6817** |
| `burnoff_k0` | 0.8583 | **0.3987** |
| gas MAPE | 17.55% | 15.03%（僅改善 2.5 點） |

擬合器把燃燒效率**腰斬**、燒損係數**腰斬**——因為它是在幻影熱源（47% 燃料當量）存在的情況下擬合的，只能用「燃燒效率變差」去抵銷「憑空多出來的熱」。得到的常數在物理上沒有意義，而且在 A 修好之後會**雙重錯誤**。

**所以：重新校正是最後一步，不是第一步。**

### 5.1 階段規劃與驗收條件

| 階段 | 內容 | 驗收條件（可證偽） |
|---|---|---|
| **P0 止血**（0.5 天） | 在軌跡加上物理硬約束並讓它**報錯而非靜默**：湯溫上限（如 `liquidus + 200`）觸發即 raise/警告；UI 明示目前輸出不可用於決策 | 現有 4 個 charge 情境（55/60/65/70 t）全部觸發告警；不再有任何 >900 °C 的湯溫被靜默輸出 |
| **P1 守恆重寫**（2–3 天） | ① 氧化放熱拆為 `f_bath`（進熔池）+ `1−f_bath`（進煙氣，並提高煙氣溫度／降低有效熱回收）；`f_bath` 是**一個有物理界限的參數**，不是自由旋鈕；② 每步扣除氧化金屬質量；③ 三個 dross 係數統一為單一金屬平衡定義（建議：`dross_kg` = 淨消失金屬，成本端係數 = 1.0，放熱端係數 = 氧化比例，另立回收爐信用為獨立項）| 65 t 爐次終點湯溫 **780 ± 20 °C**；`q_ox / q_fuel ≤ 10%`；質量平衡殘差 <0.1%；55→70 t 掃描終點湯溫變化 <30 °C（消除質量相依失控） |
| **P2 損失預算重構**（3–5 天） | 把 §3 的項目**移進軌跡**：開門（含冷風滲入）、洩漏、等待段、換向 purge、爐襯蓄熱、煙氣顯熱明算。`combustion_efficiency()` 由線性擬合改為煙氣焓平衡（給定過剩空氣、煙氣溫度、再生器有效度）| 65 t/6.5 h 情境總燃氣落在 **70 ± 10 Nm³/t**；煙氣焓平衡推得的預熱空氣溫度 **恆低於煙氣溫度**（B1 消失）；`residual_error_gj` 由獨立計算得出而非恆等式 |
| **P3 SCADA 動態辨識**（3–5 天） | 用 20 爐 5 秒資料（TT201/TT200/FT211+FT215）做**軌跡對軌跡**的辨識，取代 per-heat 總量擬合。先做可辨識性分析（決定哪些參數能同時辨識） | 20 爐 replay 的 TT200 RMSE < 25 °C、燃氣總量 MAPE < 12%；並輸出每個參數的可辨識性判定（哪些必須固定） |
| **P4 目標函數與不確定度**（2 天） | ① 過剩空氣加物理下界與 CO 懲罰（B5）；② `is_flat_bath` 2.5× 改為連續函數（B6③）；③ Ea 用文獻界（80–200 kJ/mol）做敏感度帶，不再單點；④ UI 的節省金額改為**區間**，並標註燒損項不可證偽 | 過剩空氣最佳解不再貼網格邊界；燒損率對頂溫連續可微；UI 顯示的節省帶寬涵蓋 P3 的 MAPE |
| **P5 重新校正**（1 天） | 到這一步才重跑 `calibration.py`，且以 SCADA 為主資料集 | `efficiency_scale` 落在 [0.85, 1.15]（若仍需要 >1.2 或 <0.8 的倍數，代表結構仍缺項，退回 P2） |

### 5.2 建議的優先序調整

若時間有限，**P0 + P3 的分岔確認（§3 最後一段的蓄熱室冷端溫度）先做**。理由：如果那 4.7 MW 確實集中在爐門與洩漏而非煙囪，那麼「三階段動態升溫曲線最佳化」這條技術路線本身的節能上限就有限，該優先做的是爐門管理與氣密——這是「手段 vs 目的」層級的判斷，比修任何一行程式碼都重要，而且用既有資料一天內就能判定。

---

## 附錄 A：與 `PHYSICS_MODEL_REVIEW.md`（同日較早版）的關係

| 該文項目 | 現況 |
|---|---|
| #1 燒損模型誤差 ≥ 訊號本身 | **仍成立且加強**：現在可以說得更硬——目標本身不可證偽（本文 §4） |
| #2 氧化放熱只進 Sankey 不進最佳化 | **已被 `29234a7` 修改，但修法本身就是本文的根因 A**。該項應視為「已處理但引入更嚴重缺陷」 |
| #3 `dross_cost` 全額鋁價無回收折減 | **已被 `29234a7` 加入 `dross_net_loss_factor=0.40` 處理，但造成新的單位錯配**（本文 B2）。目前是**低估** 2.5×，方向與原文相反 |
| #4 效率被夾在 0.68 天花板 | **數值已過時**（clamp 現為 0.32/0.78、base 0.35/0.75），但**結構問題仍在**（本文 B4） |
| 附註：文件與程式碼落差 | 仍成立，且新增一項：`config/furnace_parameters.json` 與 `calibrated_constants.json` 互相矛盾（本文 B3） |

## 附錄 B：審查期間的並行未提交修改

主 checkout `src/optimizer.py` 於 16:15 有未提交修改，觸及本文錯誤 A：

- `q_ox_step_kj = dross_step_kg * 0.20 * 31050.0 * 0.30` —— 等於把係數從 0.60 降到 **0.06（10 倍）**
- 新增 `effective_sp_roof = min(sp_roof, target_bath_temp_c + 20.0)` 高限保護
- 對流係數隨燃燒率縮放

**評價**：方向正確（氧化放熱確實大部分隨煙氣走），但這是**參數補丁不是守恆修復**——
1. `0.20 × 0.30` 兩個係數都沒有物理來源，只是把幻影熱源調小到看不見；
2. **質量仍未扣除**（本文 1.1 缺失 2）；
3. 高限保護是在**控制層**壓住症狀，正回饋機制本身仍在（換一組參數或更小裝料量仍可能失控）；
4. 三個 dross 係數的定義矛盾（B2）沒有解決，反而多了第四個。

建議把它併入 P1 的正式修法，而不是當成已解決。
